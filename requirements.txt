# Phase 0 — minimal setup, just enough to verify env works
# More dependencies added per phase as needed

# Web framework (will use in Phase 4)
fastapi
uvicorn

# Frontend (will use in Phase 4)
streamlit

# Environment management
python-dotenv

# Dev tooling
pytest
ruff
pre-commit